FROM alpine:latest

ARG AUTH_ENCRYPT_PATH
ARG JAR_PLUGIN_PATH
ARG HYTALE_RELEASE_PATH
ARG BASEDIR
ARG SERVER_NAME
ARG BACKEND
ARG RAM_ALLOC
ARG API_KEY

RUN apk update && apk upgrade && apk add --no-cache openjdk25-jdk unzip
RUN unzip -o ${HYTALE_RELEASE_PATH} -d ${BASEDIR}

ENV AUTH_ENCRYPT_PATH=${AUTH_ENCRYPT_PATH}
ENV JAR_PLUGIN_PATH=${JAR_PLUGIN_PATH}
ENV HYTALE_RELEASE_PATH=${HYTALE_RELEASE_PATH}
ENV BASEDIR=${BASEDIR}
ENV SERVER_NAME=${SERVER_NAME}
ENV BACKEND=${BACKEND}
ENV RAM_ALLOC=${RAM_ALLOC}
ENV API_KEY=${API_KEY}

COPY entrypoint.sh /usr/bin/entrypoint.sh

ENTRYPOINT [ "entrypoint.sh" ]

EXPOSE 5520
